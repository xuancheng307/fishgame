# 🔧 參數名稱統一修正報告

## 📋 修正原則
**以實際資料庫命名為準**，確保系統各層級參數名稱一致

## 🗂️ 實際資料庫欄位 (來源: fishmarket_game_latest.sql)

### game_participants 表
```sql
current_budget     -- 當前預算/現金 (NOT current_cash)
total_loan         -- 總借貸
total_loan_principal -- 借貸本金
fish_a_inventory   -- A魚庫存
fish_b_inventory   -- B魚庫存
cumulative_profit  -- 累積收益
```

## ❌ 需要修正的不一致

### 1. complete_database_structure.sql
- **錯誤**：`current_cash` (第84行)
- **應改為**：`current_budget`
- **原因**：與實際資料庫不符

### 2. simple-team.html (學生介面)
- **HTML元素ID錯誤**：
  - `id="currentCash"` → 應改為 `id="currentBudget"`
- **JavaScript對應**：
  - `document.getElementById('currentCash')` → 改為 `document.getElementById('currentBudget')`
- **位置**：第374行、766行

### 3. admin.html (管理介面)
- **需確認使用正確的欄位名**：`current_budget` (非 current_cash)

## ✅ 已正確的參數對應

### 後端 server.js
- 資料庫查詢：使用 `current_budget` ✅
- API回傳：轉換為 `currentBudget` (駝峰命名) ✅

### 前端 JavaScript 變數
- `financials.currentBudget` ✅ (符合API回傳格式)
- `financials.totalLoan` ✅
- `financials.fishAInventory` ✅
- `financials.fishBInventory` ✅

## 📝 命名規範總結

| 層級 | 命名規範 | 範例 |
|------|---------|------|
| **資料庫** | snake_case | current_budget, total_loan, fish_a_inventory |
| **後端API回傳** | camelCase | currentBudget, totalLoan, fishAInventory |
| **前端HTML ID** | camelCase | currentBudget, totalLoan, fishAInventory |
| **前端JS變數** | camelCase | financials.currentBudget |

## 🛠️ 修正清單

### 立即修正
1. ✅ complete_database_structure.sql - 將 current_cash 改為 current_budget
2. ✅ simple-team.html - 將 currentCash 改為 currentBudget
3. ✅ 確認 admin.html 使用正確的欄位名

### 驗證步驟
1. 檢查所有 SQL 檔案的一致性
2. 測試前端顯示是否正常
3. 確認 API 資料傳遞正確

## 📌 重要提醒
- **資料庫為準**：所有參數命名以實際資料庫欄位為準
- **轉換規則**：資料庫 snake_case → API/前端 camelCase
- **文件同步**：修正後需更新相關文件

---
**修正日期**：2025-09-12  
**負責人**：系統管理員